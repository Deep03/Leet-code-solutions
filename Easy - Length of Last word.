# Ask the user for the string.
string = input(str("Enter a sentence:"))
# Split the string about  space.
list1 = string.split(" ")
# Store the last word in a variable
last_word = list1[len(list1) - 1]
#  To count the characters of the last word, convert the last word into a list
list2 = list(last_word)

print(f"The last word is {last_word}, and the length is {len(list2)}.")
